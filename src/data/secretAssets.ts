/**
 * Optional Columbina gifs for the Konami easter egg.
 *
 * The site ships WITHOUT bundling any HoYoverse imagery. To add your own, drop
 * gif/webp/png files into `public/assets/secrets/` and list their paths here —
 * they'll rain from the sky when the Konami code is entered. If this array is
 * empty, the effect gracefully falls back to an animated SVG dove.
 *
 * Example:
 *   export const COLUMBINA_GIFS = ["/assets/secrets/columbina-hum.gif"];
 */
export const COLUMBINA_GIFS: string[] = [];
